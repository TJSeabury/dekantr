// create a tracker for currently active threads and available threads

// listen for new functions

// if a new function is received: dispatch it to an available thread

// listen for responses from dispatched threads

// when a response is received update the thread tracker
// then return the thread's response to the main script

// idle

function Threader() {
    "use strict";

}